package nameservice

import (
	"testing"
)

func TestMain(m *testing.M) {
	/*
		ns := New("bestNodeEver", []string{"172.26.1.1:2379"})
		addr, _ := replication.ParseAddress("localhost")
		ns.RegisterSelf(addr, 8080)

		exists, err := ns.ExistsKeygroup(commons.KeygroupName("test"))
		log.Info().Err(err).Msgf("Exists before: %t", exists)

		err = ns.CreateKeygroup("test")
		log.Info().Err(err).Msg("Keygroup created")

		exists, err = ns.ExistsKeygroup(commons.KeygroupName("test"))
		log.Info().Err(err).Msgf("Exists after: %t", exists)

		mem, err := ns.GetKeygroupMembers("test", false)
		log.Info().Err(err).Msgf("Members: %#v", mem)

		addr, port, err := ns.GetNodeAdress("bestNodeEver")
		log.Info().Err(err).Msgf("Adress: %s, %d", addr.Addr, port)
	*/

}
